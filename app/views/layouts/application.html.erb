<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>つくろか。</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "home", "data-turbo-track": "reload", defer: true %>
    <%= javascript_importmap_tags %>

    <!-- 任意：フォント -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  </head>

  <body id="app">
    <!-- Header -->
    <header class="site site-header">
      <div class="container header-inner">
        <%= link_to "つくろか。", root_path, class: "brand" %>

        <nav class="site-nav">
          <% if user_signed_in? %>
            <%= link_to "ログアウト", destroy_user_session_path,
                  data: { turbo_method: :delete }, class: "nav-link" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "nav-link" %>
            <%= link_to "新規登録", new_user_registration_path, class: "nav-link" %>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- Flash -->
    <div class="container flash-wrap">
      <% flash.each do |k,v| %>
        <div class="flash <%= k.to_s == 'notice' ? 'is-success' : 'is-danger' %>"><%= v %></div>
      <% end %>
    </div>

    <!-- Main -->
    <main class="site">
      <div class="container main-content">
        <%= yield %>
      </div>
    </main>

    <!-- Footer -->
    <footer class="site site-footer">
      <div class="inner">
        &copy; <%= Time.zone.now.year %> つくろか。 All rights reserved.
      </div>
    </footer>
  </body>
</html>
